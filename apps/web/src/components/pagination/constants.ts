/** Default page size for server-paginated admin / dashboard lists. */
export const ADMIN_LIST_PER_PAGE = 50;

/** Unassigned candidates loaded into the shift assign sheet (plus everyone already assigned). */
export const SHIFT_ASSIGN_STAFF_LIMIT = 8;

/** Qualified swap targets loaded into the staff coverage sheet. Search to find someone else. */
export const SWAP_CANDIDATE_LIMIT = 8;

/** Locations loaded into the admin schedules location sheet. Search to find another. */
export const LOCATION_PICKER_LIMIT = 8;
