/** Soft weekly load before overtime. Hover copy calls this the recommended cap. */
export const WEEKLY_HOURS_RECOMMENDED = 35;
/** Scheduled hours at or above this count as over the weekly limit. */
export const WEEKLY_HOURS_LIMIT = 40;
